<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.steveatw.iconnectvendor">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CustomerAdmitListActivity"
            android:label="@string/title_activity_customer_admit"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdmitCustomer"
            android:label="@string/title_activity_admit_customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VerifyCustomer"
            android:label="@string/title_activity_admit_customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BarcodeCaptureActivity"
            android:label="Read Barcode" />
        <activity
            android:name=".CustomerListActivity"
            android:label="Customer List" />
        <activity
            android:name=".ApproveCustomer"
            android:label="Approve Customer" />
    </application>

</manifest>